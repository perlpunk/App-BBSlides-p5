#!/usr/bin/env perl
use strict;
use warnings;
use 5.010;

use App::BBSlides;
use YAML::XS qw/ LoadFile /;
use Data::Dumper;
use Getopt::Long;

unless (@ARGV) {
    die "Usage: $0 slides.yaml --out output-directory";
}
my ($source) = @ARGV;
GetOptions(
    "out=s" => \my $out,
);
unless ($out) {
    die "Usage: $0 slides.yaml --out output-directory";
}
my @slides = LoadFile($source);

my $bbs = App::BBSlides->new({
    slides => [@slides],
    output => $out,
    source => $source,
});

$bbs->write;

